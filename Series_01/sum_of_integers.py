# read two terms
m = input('Provide an integer: ')
n = input('Provide another integer: ')

# convert the strings to integers
m = int(m)
n = int(n)

# compute sum of two integers
total = m + n

# write sum to output
print(total)